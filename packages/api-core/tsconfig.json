{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "rootDir": "src",
    "outDir": "dist",
    // Server-side package: no DOM, and Node globals (process, Buffer) are in play.
    "lib": ["ES2020"],
    "types": ["node"]
  },
  "include": ["src"]
}
