# @partrunner-ai/adoption-check

## 1.0.0

### Major Changes

- First public npm release. The adoption gates previously reached wave-1 apps as a packed preview tarball devDependency; the package now installs anonymously from registry.npmjs.org like every other platform package, with dual ESM/CommonJS entries, TypeScript declarations, and the `pr-adoption-check` bin.
